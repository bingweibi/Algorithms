package Jianzhioffer;

/**
 * 输入一棵二叉树，求该树的深度。
 * 从根结点到叶结点依次经过的结点（含根、叶结点）形成树的一条路径，最长路径的长度为树的深度。
 */

public class Num38 {
//    public int TreeDepth(TreeNode root) {
//
//        TreeNode p = root;
//        if (root == null){
//            return 0;
//        }
//        int deepth  = TreeDepth(p.left);
//        int deepth2  =  TreeDepth(p.right);
//        return Math.max(deepth,deepth2)+1;
//    }
}

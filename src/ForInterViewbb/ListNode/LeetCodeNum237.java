package ForInterViewbb.ListNode;

/**
 * @author bibingwei
 */
public class LeetCodeNum237 {

      public class ListNode {
          int val;
          ListNode next;
          ListNode(int x) { val = x; }
      }

    public void deleteNode(ListNode node) {

//          ListNode temp = head.next;
//          if ( head.val == node.val){
//              head = head.next;
//          }
//        while (temp != null){
//            if (temp.val == node.val){
//                head.next = temp.next;
//                temp.next = null;
//            }
//            temp = temp.next;
//            head = head.next;
//        }
    }
}
